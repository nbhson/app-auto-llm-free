import { providerIds } from "../providers/registry.js";

console.warn("Sync providers stub — will fetch LiteLLM pricing + provider /models in P2");
console.warn("Providers:", providerIds.join(", "));
