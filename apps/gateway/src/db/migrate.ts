import { config } from "../config.js";

console.log("Migrate stub — Drizzle SQLite migration will run here in P2");
console.log("DATABASE_URL:", config.databaseUrl);
console.log("Run: npx drizzle-kit generate && npx drizzle-kit migrate");
